stream {
    upstream mqtt_ssl {
        hash $remote_addr consistent;
        server mqtt5:8080;
    }

    server {
        listen 8080;
        listen [::]:8080;
        server_name mqtt.exploit4all.com;
        
        proxy_pass mqtt_ssl;
    }
    
}
